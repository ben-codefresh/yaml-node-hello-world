version: '1.0'

steps:

  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-3
    dockerfile: Dockerfile
    tag: latest
    build-arguments:
      - argument=dolphin
  
  check-limit:
    type: image-validator
    image: ${{build-prj}} #check specific image or from candidate  ${{KEY}}
    max-size: 559726453
    min-size: 550564664
    
