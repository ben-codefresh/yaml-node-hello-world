version: '1.0'

steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/counting
    dockerfile: Dockerfile
  count:
    image: ${{build-prj}}
    commands:
      - sh ./count.sh
  count2:
    image: ${{build-prj}}
    commands:
      - sh ./count.sh
  count3:
    image: ${{build-prj}}
    commands:
      - sh ./count.sh
  count4:
    image: ${{build-prj}}
    commands:
      - sh ./count.sh
